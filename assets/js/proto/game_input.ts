// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.1
// source: game_input.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "boxland.game";

export interface Input {
  clientTimeMs: number;
  move?: Input_Move | undefined;
}

export interface Input_Move {
  dx: number;
  dy: number;
}

function createBaseInput(): Input {
  return { clientTimeMs: 0, move: undefined };
}

export const Input: MessageFns<Input> = {
  encode(message: Input, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientTimeMs !== 0) {
      writer.uint32(8).uint64(message.clientTimeMs);
    }
    if (message.move !== undefined) {
      Input_Move.encode(message.move, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Input {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.clientTimeMs = longToNumber(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.move = Input_Move.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Input {
    return {
      clientTimeMs: isSet(object.clientTimeMs)
        ? globalThis.Number(object.clientTimeMs)
        : isSet(object.client_time_ms)
        ? globalThis.Number(object.client_time_ms)
        : 0,
      move: isSet(object.move) ? Input_Move.fromJSON(object.move) : undefined,
    };
  },

  toJSON(message: Input): unknown {
    const obj: any = {};
    if (message.clientTimeMs !== 0) {
      obj.clientTimeMs = Math.round(message.clientTimeMs);
    }
    if (message.move !== undefined) {
      obj.move = Input_Move.toJSON(message.move);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Input>, I>>(base?: I): Input {
    return Input.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Input>, I>>(object: I): Input {
    const message = createBaseInput();
    message.clientTimeMs = object.clientTimeMs ?? 0;
    message.move = (object.move !== undefined && object.move !== null)
      ? Input_Move.fromPartial(object.move)
      : undefined;
    return message;
  },
};

function createBaseInput_Move(): Input_Move {
  return { dx: 0, dy: 0 };
}

export const Input_Move: MessageFns<Input_Move> = {
  encode(message: Input_Move, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dx !== 0) {
      writer.uint32(8).sint32(message.dx);
    }
    if (message.dy !== 0) {
      writer.uint32(16).sint32(message.dy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Input_Move {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInput_Move();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.dx = reader.sint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.dy = reader.sint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Input_Move {
    return {
      dx: isSet(object.dx) ? globalThis.Number(object.dx) : 0,
      dy: isSet(object.dy) ? globalThis.Number(object.dy) : 0,
    };
  },

  toJSON(message: Input_Move): unknown {
    const obj: any = {};
    if (message.dx !== 0) {
      obj.dx = Math.round(message.dx);
    }
    if (message.dy !== 0) {
      obj.dy = Math.round(message.dy);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Input_Move>, I>>(base?: I): Input_Move {
    return Input_Move.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Input_Move>, I>>(object: I): Input_Move {
    const message = createBaseInput_Move();
    message.dx = object.dx ?? 0;
    message.dy = object.dy ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
