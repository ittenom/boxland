// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class InteractPayload {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):InteractPayload {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsInteractPayload(bb:flatbuffers.ByteBuffer, obj?:InteractPayload):InteractPayload {
  return (obj || new InteractPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsInteractPayload(bb:flatbuffers.ByteBuffer, obj?:InteractPayload):InteractPayload {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new InteractPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

targetEntityId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

hasWorldPos():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

worldX():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

worldY():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startInteractPayload(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addTargetEntityId(builder:flatbuffers.Builder, targetEntityId:bigint) {
  builder.addFieldInt64(0, targetEntityId, BigInt('0'));
}

static addHasWorldPos(builder:flatbuffers.Builder, hasWorldPos:boolean) {
  builder.addFieldInt8(1, +hasWorldPos, +false);
}

static addWorldX(builder:flatbuffers.Builder, worldX:number) {
  builder.addFieldInt32(2, worldX, 0);
}

static addWorldY(builder:flatbuffers.Builder, worldY:number) {
  builder.addFieldInt32(3, worldY, 0);
}

static endInteractPayload(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createInteractPayload(builder:flatbuffers.Builder, targetEntityId:bigint, hasWorldPos:boolean, worldX:number, worldY:number):flatbuffers.Offset {
  InteractPayload.startInteractPayload(builder);
  InteractPayload.addTargetEntityId(builder, targetEntityId);
  InteractPayload.addHasWorldPos(builder, hasWorldPos);
  InteractPayload.addWorldX(builder, worldX);
  InteractPayload.addWorldY(builder, worldY);
  return InteractPayload.endInteractPayload(builder);
}
}
