// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { AudioEvent } from '../../boxland/proto/audio-event.js';
import { ChunkVersion } from '../../boxland/proto/chunk-version.js';
import { EntityState } from '../../boxland/proto/entity-state.js';
import { HudDataDelta } from '../../boxland/proto/hud-data-delta.js';
import { LightingCell } from '../../boxland/proto/lighting-cell.js';
import { ProtocolVersion } from '../../boxland/proto/protocol-version.js';
import { Tile } from '../../boxland/proto/tile.js';


export class Diff {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Diff {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDiff(bb:flatbuffers.ByteBuffer, obj?:Diff):Diff {
  return (obj || new Diff()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDiff(bb:flatbuffers.ByteBuffer, obj?:Diff):Diff {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Diff()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

protocolVersion(obj?:ProtocolVersion):ProtocolVersion|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new ProtocolVersion()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

mapId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

tick():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

added(index: number, obj?:EntityState):EntityState|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new EntityState()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

addedLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

removed(index: number):bigint|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : BigInt(0);
}

removedLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

moved(index: number, obj?:EntityState):EntityState|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new EntityState()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

movedLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

tileChanges(index: number, obj?:Tile):Tile|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new Tile()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

tileChangesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

lightingChanges(index: number, obj?:LightingCell):LightingCell|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? (obj || new LightingCell()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

lightingChangesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

audio(index: number, obj?:AudioEvent):AudioEvent|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? (obj || new AudioEvent()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

audioLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

chunks(index: number, obj?:ChunkVersion):ChunkVersion|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? (obj || new ChunkVersion()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

chunksLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

hudData(index: number, obj?:HudDataDelta):HudDataDelta|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? (obj || new HudDataDelta()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

hudDataLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startDiff(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addProtocolVersion(builder:flatbuffers.Builder, protocolVersionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, protocolVersionOffset, 0);
}

static addMapId(builder:flatbuffers.Builder, mapId:number) {
  builder.addFieldInt32(1, mapId, 0);
}

static addTick(builder:flatbuffers.Builder, tick:bigint) {
  builder.addFieldInt64(2, tick, BigInt('0'));
}

static addAdded(builder:flatbuffers.Builder, addedOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, addedOffset, 0);
}

static createAddedVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAddedVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addRemoved(builder:flatbuffers.Builder, removedOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, removedOffset, 0);
}

static createRemovedVector(builder:flatbuffers.Builder, data:bigint[]):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt64(data[i]!);
  }
  return builder.endVector();
}

static startRemovedVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static addMoved(builder:flatbuffers.Builder, movedOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, movedOffset, 0);
}

static createMovedVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startMovedVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTileChanges(builder:flatbuffers.Builder, tileChangesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, tileChangesOffset, 0);
}

static createTileChangesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTileChangesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addLightingChanges(builder:flatbuffers.Builder, lightingChangesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, lightingChangesOffset, 0);
}

static createLightingChangesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startLightingChangesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addAudio(builder:flatbuffers.Builder, audioOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, audioOffset, 0);
}

static createAudioVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAudioVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addChunks(builder:flatbuffers.Builder, chunksOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, chunksOffset, 0);
}

static createChunksVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startChunksVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addHudData(builder:flatbuffers.Builder, hudDataOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, hudDataOffset, 0);
}

static createHudDataVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startHudDataVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endDiff(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // protocol_version
  return offset;
}

static createDiff(builder:flatbuffers.Builder, protocolVersionOffset:flatbuffers.Offset, mapId:number, tick:bigint, addedOffset:flatbuffers.Offset, removedOffset:flatbuffers.Offset, movedOffset:flatbuffers.Offset, tileChangesOffset:flatbuffers.Offset, lightingChangesOffset:flatbuffers.Offset, audioOffset:flatbuffers.Offset, chunksOffset:flatbuffers.Offset, hudDataOffset:flatbuffers.Offset):flatbuffers.Offset {
  Diff.startDiff(builder);
  Diff.addProtocolVersion(builder, protocolVersionOffset);
  Diff.addMapId(builder, mapId);
  Diff.addTick(builder, tick);
  Diff.addAdded(builder, addedOffset);
  Diff.addRemoved(builder, removedOffset);
  Diff.addMoved(builder, movedOffset);
  Diff.addTileChanges(builder, tileChangesOffset);
  Diff.addLightingChanges(builder, lightingChangesOffset);
  Diff.addAudio(builder, audioOffset);
  Diff.addChunks(builder, chunksOffset);
  Diff.addHudData(builder, hudDataOffset);
  return Diff.endDiff(builder);
}
}
