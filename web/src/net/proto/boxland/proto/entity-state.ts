// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class EntityState {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):EntityState {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEntityState(bb:flatbuffers.ByteBuffer, obj?:EntityState):EntityState {
  return (obj || new EntityState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEntityState(bb:flatbuffers.ByteBuffer, obj?:EntityState):EntityState {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new EntityState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

id():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

typeId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

x():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

y():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

facing():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

animId():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

animFrame():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

variantId():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

tint():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

nameplate():string|null
nameplate(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
nameplate(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

hpPct():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 255;
}

static startEntityState(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addId(builder:flatbuffers.Builder, id:bigint) {
  builder.addFieldInt64(0, id, BigInt('0'));
}

static addTypeId(builder:flatbuffers.Builder, typeId:number) {
  builder.addFieldInt32(1, typeId, 0);
}

static addX(builder:flatbuffers.Builder, x:number) {
  builder.addFieldInt32(2, x, 0);
}

static addY(builder:flatbuffers.Builder, y:number) {
  builder.addFieldInt32(3, y, 0);
}

static addFacing(builder:flatbuffers.Builder, facing:number) {
  builder.addFieldInt8(4, facing, 0);
}

static addAnimId(builder:flatbuffers.Builder, animId:number) {
  builder.addFieldInt16(5, animId, 0);
}

static addAnimFrame(builder:flatbuffers.Builder, animFrame:number) {
  builder.addFieldInt16(6, animFrame, 0);
}

static addVariantId(builder:flatbuffers.Builder, variantId:number) {
  builder.addFieldInt16(7, variantId, 0);
}

static addTint(builder:flatbuffers.Builder, tint:number) {
  builder.addFieldInt32(8, tint, 0);
}

static addNameplate(builder:flatbuffers.Builder, nameplateOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, nameplateOffset, 0);
}

static addHpPct(builder:flatbuffers.Builder, hpPct:number) {
  builder.addFieldInt8(10, hpPct, 255);
}

static endEntityState(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createEntityState(builder:flatbuffers.Builder, id:bigint, typeId:number, x:number, y:number, facing:number, animId:number, animFrame:number, variantId:number, tint:number, nameplateOffset:flatbuffers.Offset, hpPct:number):flatbuffers.Offset {
  EntityState.startEntityState(builder);
  EntityState.addId(builder, id);
  EntityState.addTypeId(builder, typeId);
  EntityState.addX(builder, x);
  EntityState.addY(builder, y);
  EntityState.addFacing(builder, facing);
  EntityState.addAnimId(builder, animId);
  EntityState.addAnimFrame(builder, animFrame);
  EntityState.addVariantId(builder, variantId);
  EntityState.addTint(builder, tint);
  EntityState.addNameplate(builder, nameplateOffset);
  EntityState.addHpPct(builder, hpPct);
  return EntityState.endEntityState(builder);
}
}
