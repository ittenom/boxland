// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { LightingCellPayload } from '../../boxland/proto/lighting-cell-payload.js';


export class PlaceLightingPayload {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PlaceLightingPayload {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPlaceLightingPayload(bb:flatbuffers.ByteBuffer, obj?:PlaceLightingPayload):PlaceLightingPayload {
  return (obj || new PlaceLightingPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPlaceLightingPayload(bb:flatbuffers.ByteBuffer, obj?:PlaceLightingPayload):PlaceLightingPayload {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PlaceLightingPayload()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

mapId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

cells(index: number, obj?:LightingCellPayload):LightingCellPayload|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new LightingCellPayload()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

cellsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startPlaceLightingPayload(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addMapId(builder:flatbuffers.Builder, mapId:number) {
  builder.addFieldInt32(0, mapId, 0);
}

static addCells(builder:flatbuffers.Builder, cellsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, cellsOffset, 0);
}

static createCellsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCellsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endPlaceLightingPayload(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPlaceLightingPayload(builder:flatbuffers.Builder, mapId:number, cellsOffset:flatbuffers.Offset):flatbuffers.Offset {
  PlaceLightingPayload.startPlaceLightingPayload(builder);
  PlaceLightingPayload.addMapId(builder, mapId);
  PlaceLightingPayload.addCells(builder, cellsOffset);
  return PlaceLightingPayload.endPlaceLightingPayload(builder);
}
}
