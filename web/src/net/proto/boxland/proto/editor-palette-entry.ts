// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class EditorPaletteEntry {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):EditorPaletteEntry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEditorPaletteEntry(bb:flatbuffers.ByteBuffer, obj?:EditorPaletteEntry):EditorPaletteEntry {
  return (obj || new EditorPaletteEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEditorPaletteEntry(bb:flatbuffers.ByteBuffer, obj?:EditorPaletteEntry):EditorPaletteEntry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new EditorPaletteEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

entityTypeId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

class_():string|null
class_(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
class_(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

spriteUrl():string|null
spriteUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
spriteUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

atlasIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

atlasCols():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

tileSize():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

folderId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

proceduralInclude():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startEditorPaletteEntry(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addEntityTypeId(builder:flatbuffers.Builder, entityTypeId:bigint) {
  builder.addFieldInt64(0, entityTypeId, BigInt('0'));
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nameOffset, 0);
}

static addClass(builder:flatbuffers.Builder, class_Offset:flatbuffers.Offset) {
  builder.addFieldOffset(2, class_Offset, 0);
}

static addSpriteUrl(builder:flatbuffers.Builder, spriteUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, spriteUrlOffset, 0);
}

static addAtlasIndex(builder:flatbuffers.Builder, atlasIndex:number) {
  builder.addFieldInt32(4, atlasIndex, 0);
}

static addAtlasCols(builder:flatbuffers.Builder, atlasCols:number) {
  builder.addFieldInt32(5, atlasCols, 0);
}

static addTileSize(builder:flatbuffers.Builder, tileSize:number) {
  builder.addFieldInt32(6, tileSize, 0);
}

static addFolderId(builder:flatbuffers.Builder, folderId:bigint) {
  builder.addFieldInt64(7, folderId, BigInt('0'));
}

static addProceduralInclude(builder:flatbuffers.Builder, proceduralInclude:boolean) {
  builder.addFieldInt8(8, +proceduralInclude, +false);
}

static endEditorPaletteEntry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createEditorPaletteEntry(builder:flatbuffers.Builder, entityTypeId:bigint, nameOffset:flatbuffers.Offset, class_Offset:flatbuffers.Offset, spriteUrlOffset:flatbuffers.Offset, atlasIndex:number, atlasCols:number, tileSize:number, folderId:bigint, proceduralInclude:boolean):flatbuffers.Offset {
  EditorPaletteEntry.startEditorPaletteEntry(builder);
  EditorPaletteEntry.addEntityTypeId(builder, entityTypeId);
  EditorPaletteEntry.addName(builder, nameOffset);
  EditorPaletteEntry.addClass(builder, class_Offset);
  EditorPaletteEntry.addSpriteUrl(builder, spriteUrlOffset);
  EditorPaletteEntry.addAtlasIndex(builder, atlasIndex);
  EditorPaletteEntry.addAtlasCols(builder, atlasCols);
  EditorPaletteEntry.addTileSize(builder, tileSize);
  EditorPaletteEntry.addFolderId(builder, folderId);
  EditorPaletteEntry.addProceduralInclude(builder, proceduralInclude);
  return EditorPaletteEntry.endEditorPaletteEntry(builder);
}
}
