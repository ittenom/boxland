// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class EditorThemeEntry {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):EditorThemeEntry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEditorThemeEntry(bb:flatbuffers.ByteBuffer, obj?:EditorThemeEntry):EditorThemeEntry {
  return (obj || new EditorThemeEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEditorThemeEntry(bb:flatbuffers.ByteBuffer, obj?:EditorThemeEntry):EditorThemeEntry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new EditorThemeEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

role():string|null
role(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
role(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

entityTypeId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

assetUrl():string|null
assetUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
assetUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

nineSliceLeft():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

nineSliceTop():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

nineSliceRight():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

nineSliceBottom():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

width():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

height():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startEditorThemeEntry(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addRole(builder:flatbuffers.Builder, roleOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, roleOffset, 0);
}

static addEntityTypeId(builder:flatbuffers.Builder, entityTypeId:bigint) {
  builder.addFieldInt64(1, entityTypeId, BigInt('0'));
}

static addAssetUrl(builder:flatbuffers.Builder, assetUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, assetUrlOffset, 0);
}

static addNineSliceLeft(builder:flatbuffers.Builder, nineSliceLeft:number) {
  builder.addFieldInt32(3, nineSliceLeft, 0);
}

static addNineSliceTop(builder:flatbuffers.Builder, nineSliceTop:number) {
  builder.addFieldInt32(4, nineSliceTop, 0);
}

static addNineSliceRight(builder:flatbuffers.Builder, nineSliceRight:number) {
  builder.addFieldInt32(5, nineSliceRight, 0);
}

static addNineSliceBottom(builder:flatbuffers.Builder, nineSliceBottom:number) {
  builder.addFieldInt32(6, nineSliceBottom, 0);
}

static addWidth(builder:flatbuffers.Builder, width:number) {
  builder.addFieldInt32(7, width, 0);
}

static addHeight(builder:flatbuffers.Builder, height:number) {
  builder.addFieldInt32(8, height, 0);
}

static endEditorThemeEntry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createEditorThemeEntry(builder:flatbuffers.Builder, roleOffset:flatbuffers.Offset, entityTypeId:bigint, assetUrlOffset:flatbuffers.Offset, nineSliceLeft:number, nineSliceTop:number, nineSliceRight:number, nineSliceBottom:number, width:number, height:number):flatbuffers.Offset {
  EditorThemeEntry.startEditorThemeEntry(builder);
  EditorThemeEntry.addRole(builder, roleOffset);
  EditorThemeEntry.addEntityTypeId(builder, entityTypeId);
  EditorThemeEntry.addAssetUrl(builder, assetUrlOffset);
  EditorThemeEntry.addNineSliceLeft(builder, nineSliceLeft);
  EditorThemeEntry.addNineSliceTop(builder, nineSliceTop);
  EditorThemeEntry.addNineSliceRight(builder, nineSliceRight);
  EditorThemeEntry.addNineSliceBottom(builder, nineSliceBottom);
  EditorThemeEntry.addWidth(builder, width);
  EditorThemeEntry.addHeight(builder, height);
  return EditorThemeEntry.endEditorThemeEntry(builder);
}
}
