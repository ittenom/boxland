// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { EntityState } from '../../boxland/proto/entity-state.js';
import { LightingCell } from '../../boxland/proto/lighting-cell.js';
import { ProtocolVersion } from '../../boxland/proto/protocol-version.js';
import { Tile } from '../../boxland/proto/tile.js';


export class MapState {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MapState {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMapState(bb:flatbuffers.ByteBuffer, obj?:MapState):MapState {
  return (obj || new MapState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMapState(bb:flatbuffers.ByteBuffer, obj?:MapState):MapState {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MapState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('BLWO');
}

protocolVersion(obj?:ProtocolVersion):ProtocolVersion|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new ProtocolVersion()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

mapId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

instanceId():string|null
instanceId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
instanceId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

tick():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

tiles(index: number, obj?:Tile):Tile|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new Tile()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

tilesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

entities(index: number, obj?:EntityState):EntityState|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new EntityState()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

entitiesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

lighting(index: number, obj?:LightingCell):LightingCell|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new LightingCell()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

lightingLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startMapState(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addProtocolVersion(builder:flatbuffers.Builder, protocolVersionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, protocolVersionOffset, 0);
}

static addMapId(builder:flatbuffers.Builder, mapId:number) {
  builder.addFieldInt32(1, mapId, 0);
}

static addInstanceId(builder:flatbuffers.Builder, instanceIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, instanceIdOffset, 0);
}

static addTick(builder:flatbuffers.Builder, tick:bigint) {
  builder.addFieldInt64(3, tick, BigInt('0'));
}

static addTiles(builder:flatbuffers.Builder, tilesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, tilesOffset, 0);
}

static createTilesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTilesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addEntities(builder:flatbuffers.Builder, entitiesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, entitiesOffset, 0);
}

static createEntitiesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEntitiesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addLighting(builder:flatbuffers.Builder, lightingOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, lightingOffset, 0);
}

static createLightingVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startLightingVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endMapState(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // protocol_version
  builder.requiredField(offset, 8) // instance_id
  return offset;
}

static finishMapStateBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'BLWO');
}

static finishSizePrefixedMapStateBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'BLWO', true);
}

static createMapState(builder:flatbuffers.Builder, protocolVersionOffset:flatbuffers.Offset, mapId:number, instanceIdOffset:flatbuffers.Offset, tick:bigint, tilesOffset:flatbuffers.Offset, entitiesOffset:flatbuffers.Offset, lightingOffset:flatbuffers.Offset):flatbuffers.Offset {
  MapState.startMapState(builder);
  MapState.addProtocolVersion(builder, protocolVersionOffset);
  MapState.addMapId(builder, mapId);
  MapState.addInstanceId(builder, instanceIdOffset);
  MapState.addTick(builder, tick);
  MapState.addTiles(builder, tilesOffset);
  MapState.addEntities(builder, entitiesOffset);
  MapState.addLighting(builder, lightingOffset);
  return MapState.endMapState(builder);
}
}
